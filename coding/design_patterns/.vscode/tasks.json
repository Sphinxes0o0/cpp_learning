{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "cmake configure and build",
			"command": "cmake -S . -B build && cmake --build build",
			"group": "build",
			"problemMatcher": [
				"$gcc"
			]
		}
	]
}